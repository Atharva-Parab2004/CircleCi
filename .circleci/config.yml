version: 2.1

jobs:
  test:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run test
          command: python test.py

workflows:
  version: 2
  build_and_test:
    jobs:
      - test
