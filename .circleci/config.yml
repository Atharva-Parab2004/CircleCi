version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run Main Script
          command: python Main1.py

  test:
    docker:
      - image: cimg/python:3.11
    steps:
      - checkout
      - run:
          name: Run Tests
          command: python test.py

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
